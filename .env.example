# ENMS Project Configuration Template

# -- Database Settings --
# Hostname for the PostgreSQL database (e.g., 'postgres' in Docker, 'localhost' on live server)
POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# -- Application Settings --
# The public-facing base URL where the application is accessed (e.g., 'http://localhost' or 'https://your-domain.com')
APP_BASE_URL=
# The absolute file system path where generated PDF reports are stored
PDF_STORAGE_PATH=
